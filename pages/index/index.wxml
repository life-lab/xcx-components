<!--index.wxml-->
<view class="container">
  <block wx:for="{{dataList}}">
    <navigator url="{{item.nav}}" hover-class="navigator-hover">
      <view>{{item.name}}</view>
    </navigator>
  </block>
</view>
